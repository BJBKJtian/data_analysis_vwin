# 向两端填充指定字符到指定长度
a.str.center(width=10, fillchar='?')

# 在右侧填充指定字符到指定长度
a.str.ljust(width=10, fillchar='?')

# 在左侧填充指定字符到指定长度
a.str.rjust(width=10, fillchar='?')

# 在字符串前面填充0到指定长度，右对齐
a.str.zfill(width=10)

# 每隔指定个字符插入一个换行符
a.str.wrap(width=2)

# 将字符串扩展n倍
a.str.repeat(repeats=2)

# 为每一个元素指定扩展倍数
a.str.repeat(repeats=[2, 2, 2])  #列表元素与a中的元素数目一致

a.str.join(sep='?')
# 等同于
a.map(lambda x: '?'.join(x))
